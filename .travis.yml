language: java
jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7
os:
  - linux
before_install:
  - sudo add-apt-repository ppa:openjdk-r/ppa -y
  - sudo apt-get update -q
  - sudo apt-get install openjfx -y